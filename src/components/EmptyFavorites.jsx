import React from 'react'; // Import the CSS file for styling

const EmptyFavorites = () => {
  return (
    <div className="empty-favorites">
      <h2>No favorite films</h2>
      <p>Add some films to your favorites list!</p>
    </div>
  );
};

export default EmptyFavorites;
